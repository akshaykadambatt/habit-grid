import {Check,Dumbbell,Utensils,Moon,Leaf,Sprout,Minus} from 'lucide-react';
import {addDays,dayState,entryId,ruleOn,targetLabel,type Data,type Habit} from '../domain/model';
export function HabitIcon({habit}:{habit:Pick<Habit,'icon'>}) {const Icon={workout:Dumbbell,food:Utensils,sleep:Moon,leaf:Leaf,habit:Sprout}[habit.icon];return <Icon size={22} aria-hidden="true"/>;}
export function HabitRow({habit,data,today,onDetails,onCheck}:{habit:Habit;data:Data;today:string;onDetails:()=>void;onCheck:()=>void}) {
 const rule=ruleOn(habit,today)!;const state=dayState(habit,today,data.entries,today);const entry=data.entries[entryId(habit.id,today)];
 const label=state==='met'?'Goal met':state==='not-met'?'Not met today':rule.kind==='number'?targetLabel(rule):habit.icon==='leaf'?'Your 2000-calorie goal':'Not logged today';
 return <article className={`habit-row ${habit.color} ${state==='met'?'completed':''}`}><div className="habit-icon"><HabitIcon habit={habit}/></div><button className="habit-body" aria-label={`${habit.name} details`} onClick={onDetails}><strong>{habit.name}</strong><span>{label}</span><div className="mini-history" aria-label="Recent seven days">{Array.from({length:7},(_,i)=>{const date=addDays(today,i-6);const s=dayState(habit,date,data.entries,today);return <i title={`${date}: ${s}`} className={`${date===today?'current':''} mini-${s}`} key={date}/>;})}</div></button><button className={`check-control ${rule.kind==='number'?'numeric':''} ${state==='met'?'checked':''}`} aria-label={rule.kind==='number'?`Log ${habit.name}`:state==='met'?`Clear ${habit.name}`:`Complete ${habit.name}`} aria-pressed={rule.kind==='checkbox'?state==='met':undefined} onClick={onCheck}>{rule.kind==='number'?entry?.value!=null?<><strong>{entry.value}</strong><span>{rule.unit}</span></>:<>Log<br/>{rule.unit}</>:state==='not-met'?<Minus size={22}/>:<Check size={22}/>}</button></article>;
}
